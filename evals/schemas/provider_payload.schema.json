{
  "type": "object",
  "required": ["entries", "summary"],
  "properties": {
    "entries": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["date", "start", "end", "description", "hours", "project_id"],
        "properties": {
          "date": { "type": "string" },
          "start": { "type": "string" },
          "end": { "type": "string" },
          "description": { "type": "string" },
          "hours": { "type": "number" },
          "project_id": { "type": ["string", "null"] }
        },
        "additionalProperties": false
      }
    },
    "summary": {
      "type": "object",
      "required": ["total_days", "total_hours", "date_range"],
      "properties": {
        "total_days": { "type": "integer", "minimum": 0 },
        "total_hours": { "type": "number", "minimum": 0 },
        "date_range": { "type": "string" }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
